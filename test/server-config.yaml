---
debug: True
# logfile: '/tmp/telephant_server.log'
# root_path='/telephant'
listen_address: '::'
listen_port: 8080
reports_dir: 'test/reports/'
url_base: https://www.telephant.eu/
index_ip_filter: 'public'

session_secret: 'abcdefgh'

auth:
  google:
    client_id: 'xxxxxxxxxxxx'
    client_secret: 'xxxxxxxxxxxxx'

db_sqlite: 'sqlite:///./test/telephant.sqlite'
admins:
  'tmshlvck@gmail.com': 'initpassword'
